/**
 * The contributing interface. Nothing here yet.
 *
 * This is the screen a person is looking at when they hand over something that
 * matters to them. Recording audio, uploading a photograph, typing up what
 * their grandmother told them, adding a translation for somebody else's.
 *
 * It is deliberately separate from `apps/web`, which is the reading experience
 * and belongs to another team. Reading and contributing are different problems
 * with different users in different moods, and putting them in one app means
 * two teams editing the same files.
 *
 * Framework is your call. See the README.
 */

export {};
